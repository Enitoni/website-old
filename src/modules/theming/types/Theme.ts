export type Theme = {
  colors: {
    primaryFallback: string
    background: string
    primary: string
    accent: string
  }
  fontColors: {
    normal: string
  }
  shadows: {
    light: string
  }
}
