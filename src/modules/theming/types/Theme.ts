export type Theme = {
  colors: {
    primaryStrong: string
    background: string
    primary: string
    accent: string
  }
  fontColors: {
    normal: string
  }
  shadows: {
    light: string
  }
}
